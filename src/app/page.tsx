import { Button } from "@/components/ui/button";
import React from "react";

function page() {
    return (
        <div>
            <Button variant="secondary" size="sm">
                Button
            </Button>
        </div>
    );
}

export default page;
